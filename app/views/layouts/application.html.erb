<!DOCTYPE html>
<html>
  <head>
    <title>Would You Rather</title>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  </head>
  <body>
    <!-- navbar -->
    <nav class="navbar navbar-default">
    <div class="container">


      <div class="navbar_header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-elem" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

        <a href="<%= root_url %>" class="navbar-brand">Home</a>
      </div>
      <div class="collapse navbar-collapse" id="collapse-elem">
        <ul class="nav navbar-nav">
          <li><%= link_to "About" %></li>
          <li><%= link_to "Contact" %></li>
          <% if current_user %>
            <li><%= link_to "Profile", user_path(current_user) %></li>
            <% if remaining_questions.empty? %>
              <li><%= link_to "Questions", out_of_questions_path %></li>
            <% else %>
              <li><%= link_to "Questions", question_path(select_unanswered_question) %></li>
            <% end %>
          <% end %>
          <li class="dropdown">
            <a href="categories_path" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories<span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% clean_categories.each do |c| %>
                <li><%= link_to c.name, question_path(select_unanswered_question_by_category(c)) %></li>
              <% end %>
              <li role="separator" class="divider"></li>
              <% nsfw_categories.each do |c| %>
                <li><%= link_to c.name, question_path(select_unanswered_question_by_category(c)) %></li>
              <% end %>
            </ul>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <%= button_to "Log Out", logout_path, method: :delete %>
          <% else %>
            <%= link_to "Sign Up", signup_path %> |
            <%= link_to "Log In", login_path %>
          <% end %>
        </ul>
      </div>
    </div>
    </nav>

    <!-- content -->
    <div class="container"  role="main">
      <%= yield %>
    </div>

  <script src="http://code.jquery.com/jquery-3.3.1.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </body>
</html>
